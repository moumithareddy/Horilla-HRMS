export const environment = {
  production: false,
  apiUrl: '/api', // Use relative URL to call the API on the same domain
  useMockData: false // Set to false to use the real API, true for mock data
};
